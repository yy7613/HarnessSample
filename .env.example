# HarnessSample Web Search Tool settings
# Copy this file to the repository root as .env and replace the placeholders.
# For backward compatibility, scripts/.env is also loaded if present.
TAVILY_API_KEY=
TINYFISH_API_KEY=
TINYFISH_LOCATION=JP
TINYFISH_LANGUAGE=ja